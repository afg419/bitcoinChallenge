const processes = require("../../main/util/exchangeRateProcesses");
const CryptoExchangeRate = require("../../../dist/main/models/CryptoExchangeRate");


describe('Exchange rate processes', () => {
    new CryptoExchangeRate();

    it('should filter sort and group exchange rates', () => {

    });
});